# Compiled Python files
*.pyc

# Folder view configuration files
.DS_Store
Desktop.ini

# Thumbnail cache files
._*
Thumbs.db

# Files that might appear on external disks
.Spotlight-V100
.Trashes

# Claude Code local settings
**/.claude/settings.local.json
**/.claude/push-guard.json
CLAUDE.local.md
AGENTS.local.md

# Per-project setup manifests (macos-setup tasks/projects.sh) — machine-local,
# never committed. The generated mise.local.toml and .claude/settings.local.json
# are excluded per-repo via .git/info/exclude by the task; the workspace dir
# itself is typically not a git repo. Skills are no longer symlinked in — they
# are committed in the repo that uses them — so a `/.claude/skills/` line the
# retired route left in a repo's exclude file is stale, not deliberate (see
# config/agent-skills/README.md, "Per-project setup").
.tapppi-project.json
.tapppi-project.yml
.tapppi-project.yaml

# jira-cli config (server URL / login metadata, no token) written by
# `jira init -c .jira-config.yml` and pointed at via JIRA_CONFIG_FILE.
.jira-config.yml
